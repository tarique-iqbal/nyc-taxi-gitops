apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: clickhouse

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - service.yaml
  - statefulset.yaml
  - schema-apply-configmap.yaml
  - schema-apply-secret.yaml
  - schema-job.yaml

images:
  - name: app
    newName: app
    newTag: latest
