apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Layers on top of overlays/dev -- see producer/overlays/local for why.
resources:
  - ../dev

images:
  - name: app
    newName: app
    newTag: local
